package Recursion;

public class MinimumNumberSquare {
    public static int findMinimumNumS(int num, int mul){
        if(num== mul)
            return mul;
        if(num< mul)
            return 0;
        return 0;
    }
}
